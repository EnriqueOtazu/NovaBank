import 'CategoriaFooter.js'

function ProductTable({ products }) {
 const rows = [];
 let lastCategory = null;
 products.forEach((product) => {
 if (product.category !== lastCategory) {
 rows.push(
 <CategoriaFooter
 category={product.category}
 key={product.category} />
 );
 }
}
 )
}